"use client";
import {useMemo,useRef,useState} from "react";
import {getSupabase} from "@/lib/supabase";
import type {ErpData} from "../../types";
import type {InventoryUnit,SalesData} from "./types";
import {brl,statusLabel} from "./utils";
import {UnitEditor} from "./unit-editor";

export function InventoryView({data,sales,reload}:{data:ErpData;sales:SalesData;reload:()=>Promise<void>}){const[project,setProject]=useState("");const[status,setStatus]=useState("");const[search,setSearch]=useState("");const[edit,setEdit]=useState<InventoryUnit|null|"new">(null);const file=useRef<HTMLInputElement>(null);const scope=useMemo(()=>sales.units.filter(u=>!project||u.project_id===project),[sales.units,project]);const rows=useMemo(()=>scope.filter(u=>(!status||u.status===status)&&(!search||`${u.unit_code} ${u.block_code} ${u.lot_number}`.toLowerCase().includes(search.toLowerCase()))),[scope,status,search]);const signed=sales.contracts.filter(c=>c.status==="assinado"&&(!project||c.project_id===project));const signedProposalIds=new Set(signed.map(c=>c.proposal_id));const commercialized=sales.proposals.filter(p=>signedProposalIds.has(p.id));const vgvTotal=scope.reduce((s,u)=>s+Number(u.list_price||0),0);const vgvStock=scope.filter(u=>u.status!=="vendido").reduce((s,u)=>s+Number(u.list_price||0),0);const vgvSold=commercialized.reduce((s,p)=>s+Number(p.sale_price||0),0);const discounts=commercialized.reduce((s,p)=>s+Math.max(0,Number(p.list_price||0)-Number(p.sale_price||0)),0);const receivables=sales.installments.filter(i=>signedProposalIds.has(i.proposal_id)&&i.status!=="cancelada").reduce((s,i)=>s+Number(i.amount||0),0);const outstanding=sales.installments.filter(i=>signedProposalIds.has(i.proposal_id)&&i.status==="emitida"&&data.entries.find(e=>e.id===i.financial_entry_id)?.status!=="recebido").reduce((s,i)=>s+Number(i.amount||0),0);
async function importCsv(f:File){const text=await f.text();const lines=text.split(/\r?\n/).filter(Boolean);if(lines.length<2)return;const sep=lines[0].includes(";")?";":",";const headers=lines[0].split(sep).map(v=>v.trim().toLowerCase());const idx=(...names:string[])=>names.map(n=>headers.indexOf(n)).find(i=>i>=0)??-1;const projectId=project||data.projects[0]?.id;if(!projectId)throw new Error("Selecione um empreendimento");const payload=lines.slice(1).map((line,lineIndex)=>{const c=line.split(sep).map(v=>v.trim().replace(/^"|"$/g,""));const parse=(v:string)=>Number((v||"0").replace(/R\$\s?/g,"").replace(/\./g,"").replace(",","."));const area=parse(c[idx("area","area_m2")]);const sqm=parse(c[idx("valor_m2","preco_m2","preco_por_m2")]);const informedTotal=parse(c[idx("preco","valor","valor_total")]);const total=sqm&&area?sqm*area:informedTotal;const block=c[idx("quadra")]||"01";const lot=c[idx("lote")]||String(lineIndex+1);return{organization_id:data.organization.id,project_id:projectId,block_code:block,lot_number:lot,unit_code:c[idx("codigo")]||`Q${block}-L${lot}`,unit_type:c[idx("tipo")]||"lote",area,list_price:Math.round(total*100)/100,price_per_sqm:sqm||(area?total/area:null),minimum_price:parse(c[idx("preco_minimo","valor_minimo")])||null,status:(c[idx("status")]||"disponivel").toLowerCase().replaceAll(" ","_"),corner:["sim","true","1"].includes((c[idx("esquina")]||"").toLowerCase()),topography:c[idx("topografia")]||null,orientation:c[idx("orientacao")]||null,created_by:data.session.user.id};});const client=getSupabase();if(!client)return;const r=await client.from("crm_inventory_units").upsert(payload,{onConflict:"organization_id,project_id,block_code,lot_number"});if(r.error)throw r.error;await reload();}
function exportCsv(){const content=["codigo;quadra;lote;tipo;area;valor_m2;valor_total;preco_minimo;status;esquina;topografia;orientacao",...rows.map(u=>[u.unit_code,u.block_code,u.lot_number,u.unit_type,u.area,u.price_per_sqm||"",u.list_price,u.minimum_price||"",u.status,u.corner?"sim":"não",u.topography||"",u.orientation||""].join(";"))].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob(["\ufeff"+content],{type:"text/csv"}));a.download="estoque-imobiliario.csv";a.click();URL.revokeObjectURL(a.href);}
return <div className="crm5-stack"><section className="crm5-section-header"><div><small>ESTOQUE COMERCIAL</small><h2>Unidades e lotes</h2><p>Cadastro, preços por metro quadrado, disponibilidade, VGV e importação em massa.</p></div><div className="toolbar-actions"><button onClick={()=>file.current?.click()}>Importar CSV</button><button onClick={exportCsv}>Exportar</button><button className="primary" onClick={()=>setEdit("new")}>+ Unidade</button><input ref={file} hidden type="file" accept=".csv,text/csv" onChange={e=>e.target.files?.[0]&&importCsv(e.target.files[0])}/></div></section><section className="inventory-vgv-grid"><article><small>VGV total</small><strong>{brl.format(vgvTotal)}</strong><span>{scope.length} unidades cadastradas</span></article><article><small>VGV do estoque</small><strong>{brl.format(vgvStock)}</strong><span>Unidades ainda não vendidas</span></article><article><small>VGV comercializado</small><strong>{brl.format(vgvSold)}</strong><span>Contratos assinados</span></article><article><small>Descontos concedidos</small><strong>{brl.format(discounts)}</strong><span>Diferença entre tabela e contratos</span></article><article><small>Recebíveis contratuais</small><strong>{brl.format(receivables)}</strong><span>Em aberto: {brl.format(outstanding)}</span></article></section><section className="crm5-toolbar"><select value={project} onChange={e=>setProject(e.target.value)}><option value="">Todos os empreendimentos</option>{data.projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos os status</option>{["disponivel","reservado","vendido","bloqueio_estrategico","bloqueio_comercial","indisponivel"].map(v=><option key={v} value={v}>{statusLabel[v]}</option>)}</select><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar código, quadra ou lote"/><span>{rows.length} unidades</span></section><section className="sales-inventory-table"><header><span>Unidade</span><span>Empreendimento</span><span>Área</span><span>Preço</span><span>Status</span><span></span></header>{rows.map(u=><article key={u.id}><strong>{u.unit_code}<small>Q. {u.block_code} · L. {u.lot_number}</small></strong><span>{data.projects.find(p=>p.id===u.project_id)?.name||"—"}</span><span>{u.area} m²</span><span>{brl.format(Number(u.list_price))}<small>{brl.format(Number(u.price_per_sqm||0))}/m²</small></span><i data-status={u.status}>{statusLabel[u.status]}</i><button onClick={()=>setEdit(u)}>Editar</button></article>)}</section>{edit&&<UnitEditor data={data} unit={edit==="new"?null:edit} close={()=>setEdit(null)} reload={reload}/>}</div>}
