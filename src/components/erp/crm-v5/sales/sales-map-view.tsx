"use client";
import {useMemo,useState} from "react";
import {getSupabase} from "@/lib/supabase";
import type {ErpData} from "../../types";
import type {CrmSection} from "../types";
import type {InventoryUnit,SalesData} from "./types";
import {brl,statusLabel} from "./utils";
import {ReservationModal,UnitEditor} from "./unit-editor";

export function SalesMapView({data,sales,reload,setSection}:{data:ErpData;sales:SalesData;reload:()=>Promise<void>;setSection:(s:CrmSection)=>void}){const[project,setProject]=useState(data.projects[0]?.id||"");const[selected,setSelected]=useState<InventoryUnit|null>(null);const[edit,setEdit]=useState<InventoryUnit|null|"new">(null);const[reserve,setReserve]=useState<{unit:InventoryUnit;strategic?:boolean}|null>(null);const units=useMemo(()=>sales.units.filter(u=>u.project_id===project&&u.active),[sales.units,project]);const blocks=[...new Set(units.map(u=>u.block_code))].sort();const totals=Object.fromEntries(["disponivel","reservado","vendido","bloqueio_estrategico","bloqueio_comercial","indisponivel"].map(k=>[k,units.filter(u=>u.status===k).length]));async function release(unit:InventoryUnit){const client=getSupabase();if(!client)return;await client.from("crm_unit_reservations").update({status:"cancelada"}).eq("unit_id",unit.id).eq("status","ativa");await client.from("crm_inventory_units").update({status:"disponivel",strategic_reason:null,reserved_until:null}).eq("id",unit.id);await reload();setSelected(null);}function createProposal(unit:InventoryUnit){localStorage.setItem("evora-proposal-unit",unit.id);setSection("proposals" as CrmSection);}return <div className="crm5-stack"><section className="crm5-section-header"><div><small>COMERCIALIZAÇÃO IMOBILIÁRIA</small><h2>Mapa de vendas</h2><p>Estoque, disponibilidade, reservas, bloqueios e vendas por empreendimento e quadra.</p></div><div className="toolbar-actions"><select value={project} onChange={e=>setProject(e.target.value)}>{data.projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><button onClick={()=>setEdit("new")}>+ Unidade</button></div></section><section className="sales-status-summary">{Object.entries(totals).map(([key,value])=><article key={key} data-status={key}><strong>{value}</strong><span>{statusLabel[key]}</span></article>)}</section>{!units.length?<section className="crm5-empty"><strong>Nenhuma unidade cadastrada</strong><p>Cadastre manualmente ou importe o estoque pela tela Unidades.</p><button className="primary" onClick={()=>setSection("inventory" as CrmSection)}>Abrir estoque</button></section>:<section className="sales-map">{blocks.map(block=><article className="sales-block" key={block}><header><div><small>QUADRA</small><h3>{block}</h3></div><span>{units.filter(u=>u.block_code===block).length} unidades</span></header><div className="sales-lot-grid">{units.filter(u=>u.block_code===block).map(unit=><button key={unit.id} className={selected?.id===unit.id?"selected":""} data-status={unit.status} onClick={()=>setSelected(unit)} title={`${unit.unit_code} · ${brl.format(Number(unit.list_price))}`}><b>{unit.lot_number}</b><small>{unit.area} m²</small><span>{brl.format(Number(unit.list_price))}</span></button>)}</div></article>)}</section>}{selected&&<aside className="sales-unit-drawer"><button className="close" onClick={()=>setSelected(null)}>×</button><small>{statusLabel[selected.status]}</small><h3>{selected.unit_code}</h3><dl><div><dt>Quadra / lote</dt><dd>{selected.block_code} / {selected.lot_number}</dd></div><div><dt>Área</dt><dd>{selected.area} m²</dd></div><div><dt>Preço</dt><dd>{brl.format(Number(selected.list_price))}</dd></div><div><dt>Valor/m²</dt><dd>{brl.format(Number(selected.price_per_sqm||selected.list_price/Math.max(selected.area,1)))}</dd></div><div><dt>Preço mínimo</dt><dd>{selected.minimum_price?brl.format(Number(selected.minimum_price)):"Não definido"}</dd></div><div><dt>Características</dt><dd>{[selected.corner?"Esquina":null,selected.topography,selected.orientation].filter(Boolean).join(" · ")||"—"}</dd></div></dl>{selected.strategic_reason&&<p className="feedback">{selected.strategic_reason}</p>}<div className="sales-drawer-actions">{selected.status==="disponivel"&&<><button className="primary" onClick={()=>createProposal(selected)}>Criar proposta</button><button onClick={()=>setReserve({unit:selected})}>Reservar</button><button onClick={()=>setReserve({unit:selected,strategic:true})}>Bloquear</button></>}{["reservado","bloqueio_estrategico","bloqueio_comercial"].includes(selected.status)&&<button onClick={()=>release(selected)}>Liberar unidade</button>}<button onClick={()=>setEdit(selected)}>Editar cadastro</button></div></aside>}{edit&&<UnitEditor data={data} unit={edit==="new"?null:edit} close={()=>setEdit(null)} reload={reload}/>} {reserve&&<ReservationModal data={data} sales={sales} unit={reserve.unit} strategic={reserve.strategic} close={()=>setReserve(null)} reload={reload}/>}</div>}
