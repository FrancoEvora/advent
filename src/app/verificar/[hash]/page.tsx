"use client";
import {useEffect,useState} from "react";
import {useParams} from "next/navigation";
import {getSupabase} from "@/lib/supabase";
const labels:Record<string,string>={proposal:"Proposta comercial",contract:"Contrato",accepted:"Aceite",signed:"Assinatura",sent:"Envio",customer:"Comprador",company:"Évora Urbanismo"};
export default function VerifySignaturePage(){const params=useParams<{hash:string}>();const[data,setData]=useState<any>(null),[error,setError]=useState("");useEffect(()=>{const load=async()=>{const client=getSupabase();if(!client)return;const r=await client.rpc("crm_verify_signature_event",{p_evidence_hash:params.hash});if(r.error||!r.data){setError("Evidência não localizada.");return}setData(r.data)};load()},[params.hash]);if(error)return <main className="signature-verification invalid"><img src="/evora-brand.svg" alt="Évora Urbanismo"/><h1>Evidência inválida</h1><p>{error}</p></main>;if(!data)return <main className="signature-verification"><img src="/evora-brand.svg" alt="Évora Urbanismo"/><p>Verificando evidência...</p></main>;return <main className="signature-verification valid"><header><img src="/evora-brand.svg" alt="Évora Urbanismo"/><div><small>CERTIFICADO DE EVIDÊNCIA ELETRÔNICA</small><h1>Registro verificado</h1></div></header><section><span className="verification-badge">✓ Evidência íntegra na base da Évora</span><dl><div><dt>Documento</dt><dd>{labels[data.entity_type]||data.entity_type}</dd></div><div><dt>Ato</dt><dd>{labels[data.action]||data.action}</dd></div><div><dt>Parte</dt><dd>{labels[data.signer_role]||data.signer_role}</dd></div><div><dt>Signatário</dt><dd>{data.signer_name}</dd></div><div><dt>Data e hora</dt><dd>{new Date(data.created_at).toLocaleString("pt-BR")}</dd></div><div><dt>Documento conferido</dt><dd>{data.signer_document_last4?`Final ${data.signer_document_last4}`:"Não aplicável"}</dd></div><div><dt>IP registrado</dt><dd>{data.ip_recorded?"Sim":"Não"}</dd></div><div><dt>Localização consentida</dt><dd>{data.geolocation_recorded?"Sim":"Não"}</dd></div></dl><article><small>HASH DO DOCUMENTO</small><code>{data.document_hash}</code></article><article><small>HASH DA EVIDÊNCIA</small><code>{data.evidence_hash}</code></article><p>Este certificado comprova que a evidência correspondente está registrada e vinculada ao hash da versão eletrônica apresentada no momento do ato.</p></section><footer>© 2026 Évora Urbanismo · Versão 5.6 Enterprise</footer></main>}
