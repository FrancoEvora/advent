"use client";
import {useMemo,useState} from "react";import {getSupabase} from "@/lib/supabase";import type {ErpData} from "../types";import type {PostSaleData,PostSaleJourney} from "./types";import {brl,contractContext,journeyStages,stageLabel} from "./utils";
export function PostSaleOverview({data,ps,setSection}:{data:ErpData;ps:PostSaleData;setSection:(s:any)=>void}){const active=ps.contracts.filter(c=>c.status==="assinado");const contexts=active.map(c=>contractContext(data,ps,c.id));const open=contexts.reduce((s,c)=>s+c.openAmount,0);const overdue=contexts.reduce((s,c)=>s+c.overdueAmount,0);const tickets=ps.tickets.filter(t=>!["resolvido","cancelado"].includes(t.status));const avgNps=ps.surveys.length?ps.surveys.reduce((s,x)=>s+x.score,0)/ps.surveys.length:0;const deeds=ps.deeds.filter(d=>!["registrado","concluido"].includes(d.status));const risks=ps.journeys.filter(j=>["alto","critico"].includes(j.risk_level));return <div className="post-sale-stack"><section className="post-sale-hero"><div><small>PÓS-VENDA 360°</small><h2>Carteira, relacionamento e receita protegida.</h2><p>Da assinatura à quitação, entrega, escritura e relacionamento pós-entrega.</p></div><div className="post-sale-period"><button onClick={()=>setSection("portfolio")}>Abrir carteira</button><button onClick={()=>setSection("tickets")}>Central de atendimento</button></div></section><section className="post-sale-kpis"><article><small>Contratos ativos</small><strong>{active.length}</strong><span>{brl.format(contexts.reduce((s,c)=>s+Number(c.proposal?.sale_price||0),0))} contratados</span></article><article><small>Saldo a receber</small><strong>{brl.format(open)}</strong><span>{contexts.reduce((s,c)=>s+c.open.length,0)} títulos</span></article><article className={overdue>0?"risk":""}><small>Inadimplência</small><strong>{brl.format(overdue)}</strong><span>{contexts.reduce((s,c)=>s+c.overdue.length,0)} vencidos</span></article><article><small>Chamados abertos</small><strong>{tickets.length}</strong><span>{tickets.filter(t=>t.sla_due_at&&new Date(t.sla_due_at)<new Date()).length} fora do SLA</span></article><article><small>Escrituras pendentes</small><strong>{deeds.length}</strong><span>processos ativos</span></article><article><small>NPS médio</small><strong>{avgNps.toFixed(1)}</strong><span>{ps.surveys.length} respostas</span></article></section>{risks.length>0&&<section className="post-sale-alert"><strong>{risks.length} cliente(s) exigem atenção imediata.</strong><button onClick={()=>setSection("journey")}>Revisar riscos</button></section>}<section className="post-sale-grid two"><article className="post-sale-panel"><header><div><small>CARTEIRA</small><h3>Clientes por etapa</h3></div></header><div className="stage-bars">{journeyStages.map(stage=>{const n=ps.journeys.filter(j=>j.current_stage===stage).length;return <div key={stage}><span>{stageLabel[stage]}</span><i><b style={{width:`${active.length?n/active.length*100:0}%`}}/></i><strong>{n}</strong></div>})}</div></article><article className="post-sale-panel"><header><div><small>PRIORIDADES</small><h3>Próximas ações</h3></div></header><div className="post-sale-list">{ps.journeys.filter(j=>j.next_action).sort((a,b)=>(a.next_action_at||"9999").localeCompare(b.next_action_at||"9999")).slice(0,8).map(j=>{const c=contractContext(data,ps,j.contract_id);return <button key={j.id} onClick={()=>setSection("journey")}><div><strong>{c.proposal?.customer_name||c.contract?.contract_number}</strong><small>{j.next_action}</small></div><time>{j.next_action_at?new Date(j.next_action_at).toLocaleDateString("pt-BR"):"Sem data"}</time></button>})}</div></article></section></div>}
export function PortfolioView({data,ps}:{data:ErpData;ps:PostSaleData}){const[search,setSearch]=useState("");const rows=useMemo(()=>ps.contracts.map(c=>contractContext(data,ps,c.id)).filter(x=>!search||`${x.proposal?.customer_name} ${x.contract?.contract_number} ${x.unit?.unit_code}`.toLowerCase().includes(search.toLowerCase())),[ps,data,search]);return <div className="post-sale-stack"><section className="post-sale-heading"><div><small>CARTEIRA ATIVA</small><h2>Clientes e contratos</h2><p>Posição contratual, financeira, documental e de atendimento.</p></div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar cliente, contrato ou unidade"/></section><section className="post-sale-table"><header><span>Cliente / contrato</span><span>Unidade</span><span>Valor</span><span>Recebido</span><span>Em aberto</span><span>Vencido</span><span>Situação</span></header>{rows.map(x=>{const j=ps.journeys.find(k=>k.contract_id===x.contract.id);return <article key={x.contract.id}><strong>{x.proposal?.customer_name}<small>{x.contract.contract_number}</small></strong><span>{x.project?.name}<small>{x.unit?.unit_code}</small></span><span>{brl.format(Number(x.proposal?.sale_price||0))}</span><span>{brl.format(x.received)}</span><span>{brl.format(x.openAmount)}</span><span className={x.overdueAmount>0?"danger":""}>{brl.format(x.overdueAmount)}</span><i data-risk={j?.risk_level||"baixo"}>{stageLabel[j?.current_stage||x.contract.status]||x.contract.status}</i></article>})}</section></div>}
export function JourneyView({data,ps,reload}:{data:ErpData;ps:PostSaleData;reload:()=>Promise<void>}){const[edit,setEdit]=useState<PostSaleJourney|null>(null);async function save(j:PostSaleJourney,form:FormData){const client=getSupabase();if(!client)return;const stage=String(form.get("stage"));const progress=Math.max(0,Math.min(100,Number(form.get("progress"))));const r=await client.from("post_sale_journeys").update({current_stage:stage,progress_pct:progress,documentation_status:String(form.get("documentation_status")),delivery_status:String(form.get("delivery_status")),deed_status:String(form.get("deed_status")),risk_level:String(form.get("risk_level")),next_action:String(form.get("next_action")||"")||null,next_action_at:String(form.get("next_action_at")||"")||null,notes:String(form.get("notes")||"")||null,updated_at:new Date().toISOString()}).eq("id",j.id);if(r.error)throw r.error;await reload();setEdit(null)}return <div className="post-sale-stack"><section className="post-sale-heading"><div><small>JORNADA DO CLIENTE</small><h2>Etapas, pendências e riscos</h2></div></section><section className="journey-board">{ps.journeys.map(j=>{const c=contractContext(data,ps,j.contract_id);return <article key={j.id}><header><div><small>{c.contract?.contract_number}</small><h3>{c.proposal?.customer_name}</h3><span>{c.project?.name} · {c.unit?.unit_code}</span></div><i data-risk={j.risk_level}>{j.risk_level}</i></header><div className="journey-progress"><i><b style={{width:`${j.progress_pct}%`}}/></i><strong>{j.progress_pct}%</strong></div><dl><div><dt>Etapa</dt><dd>{stageLabel[j.current_stage]||j.current_stage}</dd></div><div><dt>Documentos</dt><dd>{j.documentation_status}</dd></div><div><dt>Entrega</dt><dd>{j.delivery_status}</dd></div><div><dt>Escritura</dt><dd>{j.deed_status}</dd></div></dl><footer><span>{j.next_action||"Sem próxima ação"}</span><button onClick={()=>setEdit(j)}>Atualizar</button></footer></article>})}</section>{edit&&<div className="modal-backdrop" onMouseDown={()=>setEdit(null)}><form className="modal large" onSubmit={e=>{e.preventDefault();save(edit,new FormData(e.currentTarget))}} onMouseDown={e=>e.stopPropagation()}><button type="button" className="modal-close" onClick={()=>setEdit(null)}>×</button><header><small>JORNADA</small><h2>Atualizar cliente</h2></header><div className="form-grid three"><label>Etapa<select name="stage" defaultValue={edit.current_stage}>{journeyStages.map(s=><option key={s} value={s}>{stageLabel[s]}</option>)}</select></label><label>Progresso (%)<input name="progress" type="number" min="0" max="100" defaultValue={edit.progress_pct}/></label><label>Risco<select name="risk_level" defaultValue={edit.risk_level}><option>baixo</option><option>medio</option><option>alto</option><option>critico</option></select></label><label>Documentação<select name="documentation_status" defaultValue={edit.documentation_status}><option>pendente</option><option>em_analise</option><option>completa</option><option>irregular</option></select></label><label>Entrega<select name="delivery_status" defaultValue={edit.delivery_status}><option>nao_iniciada</option><option>planejada</option><option>agendada</option><option>concluida</option></select></label><label>Escritura<select name="deed_status" defaultValue={edit.deed_status}><option>aguardando_quitacao</option><option>documentacao</option><option>cartorio</option><option>registrada</option></select></label><label className="span-2">Próxima ação<input name="next_action" defaultValue={edit.next_action||""}/></label><label>Data<input name="next_action_at" type="datetime-local" defaultValue={edit.next_action_at?.slice(0,16)||""}/></label><label className="span-3">Observações<textarea name="notes" rows={3} defaultValue={edit.notes||""}/></label></div><footer><button type="button" onClick={()=>setEdit(null)}>Cancelar</button><button className="primary">Salvar jornada</button></footer></form></div>}</div>}