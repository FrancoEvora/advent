"use client";
import {useEffect,useState} from "react";
import {useParams} from "next/navigation";
import {getSupabase} from "@/lib/supabase";
const money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"});
export default function PublicContractPage(){const params=useParams<{token:string}>();const[data,setData]=useState<any>(null);const[error,setError]=useState("");useEffect(()=>{const load=async()=>{const client=getSupabase();if(!client)return;const r=await client.rpc("crm_get_public_contract",{p_token:params.token});if(r.error||!r.data){setError("Contrato inválido, cancelado ou expirado.");return;}setData(r.data);};load();},[params.token]);if(error)return <main className="public-proposal-state"><img src="/evora-brand.svg" alt="Évora Urbanismo"/><h1>{error}</h1><p>Solicite uma nova via ao consultor responsável.</p></main>;if(!data)return <main className="public-proposal-state"><img src="/evora-brand.svg" alt="Évora Urbanismo"/><p>Carregando contrato...</p></main>;const c=data.contract,p=data.proposal,u=data.unit,project=data.project,t=data.template||{};return <main className="public-proposal public-contract"><header><img src="/evora-brand.svg" alt="Évora Urbanismo"/><div><small>CONTRATO</small><h1>{c.number}</h1><p>{project.name} · Unidade {u.code}</p></div></header><section className="public-hero"><div><small>COMPRADOR</small><h2>{p.customer_name}</h2><p>{p.customer_profile?.marital_status||"Estado civil não informado"} · {p.customer_profile?.occupation||"Profissão não informada"}</p></div><article><small>VALOR CONTRATADO</small><strong>{money.format(Number(p.sale_price))}</strong><span>Entrada em {p.down_payment_installments_count||1} parcela(s)</span></article></section><section className="public-terms"><article><small>Quadra / lote</small><strong>{u.block} / {u.lot}</strong></article><article><small>Área</small><strong>{u.area} m²</strong></article><article><small>Prazo mensal</small><strong>{p.installments_count} meses</strong></article><article><small>Atualização</small><strong>{(Number(p.monthly_interest_rate)*100).toFixed(3)}% a.m. + {p.indexer}</strong></article><article><small>Status</small><strong>{c.status}</strong></article><article><small>Assinatura</small><strong>{c.signed_at?new Date(c.signed_at).toLocaleDateString("pt-BR"):"Pendente"}</strong></article></section><section className="public-notes"><h2>{t.title||"Instrumento Particular"}</h2><p>{t.body||"Documento contratual da comercialização imobiliária."}</p>{(t.clauses||[]).map((clause:any,index:number)=><article key={index}><h3>{index+1}. {clause.title}</h3><p>{clause.text}</p></article>)}</section><section className="public-installments"><h2>Plano de pagamento</h2><div><header><span>#</span><span>Tipo</span><span>Vencimento</span><span>Valor</span></header>{(data.installments||[]).map((i:any)=><article key={`${i.type}-${i.number}`}><span>{i.number}</span><span>{i.type}</span><span>{new Date(`${i.due_date}T12:00:00`).toLocaleDateString("pt-BR")}</span><strong>{money.format(Number(i.amount))}</strong></article>)}</div></section><footer><p>Documento disponibilizado pela plataforma proprietária da Évora Urbanismo.</p><button onClick={()=>window.print()}>Imprimir ou salvar em PDF</button><small>© 2026 Évora Urbanismo</small></footer></main>}
