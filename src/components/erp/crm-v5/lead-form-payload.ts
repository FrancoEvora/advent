import type {CrmRecord,ErpData} from "../types";
import type {CrmEnterpriseData} from "./types";
import {buyerPayload} from "./buyer-profile";
export function buildLeadPayload(form:FormData,data:ErpData,crm:CrmEnterpriseData,lead:CrmRecord|null){const pipeline=crm.pipelines.find(i=>i.id===form.get("pipeline_id"))||crm.pipelines.find(i=>i.is_default);const stage=crm.stages.find(i=>i.id===form.get("stage_id"))||crm.stages.find(i=>i.pipeline_id===pipeline?.id&&i.position===1);const phone=String(form.get("phone")||"").trim();const email=String(form.get("email")||"").trim();const projectId=String(form.get("project_id")||"")||null;const budgetMax=Number(form.get("budget_max")||0);const estimate=Number(form.get("estimated_value")||0);let score=(phone?15:0)+(email?10:0)+(projectId?20:0)+(budgetMax?20:0)+(estimate?15:0)+(form.get("source")?10:0);score=Math.min(100,score);let sdr=String(form.get("sdr_user_id")||"")||null;if(!sdr){const teams=crm.teams.filter(i=>i.team_type==="sdr"&&i.active).map(i=>i.id);const candidates=crm.teamMembers.filter(i=>teams.includes(i.team_id)&&i.active);sdr=candidates.sort((a,b)=>crm.records.filter(r=>r.sdr_user_id===a.user_id&&r.record_status==="aberta").length-crm.records.filter(r=>r.sdr_user_id===b.user_id&&r.record_status==="aberta").length)[0]?.user_id||data.session.user.id;}const now=new Date();return{score,payload:{organization_id:data.organization.id,person_name:String(form.get("person_name")).trim(),company_name:String(form.get("company_name")||"")||null,email:email||null,phone:phone||null,project_id:projectId,pipeline_id:pipeline?.id||null,stage_id:stage?.id||null,stage:stage?.code||"novo",record_status:stage?.is_won?"ganha":stage?.is_lost?"perdida":"aberta",source:String(form.get("source")||"")||null,source_channel:String(form.get("source_channel")||"")||null,campaign_id:String(form.get("campaign_id")||"")||null,estimated_value:estimate,probability:Number(stage?.probability||10),expected_close_date:String(form.get("expected_close_date")||"")||null,next_action_at:String(form.get("next_action_at")||"")||null,owner_user_id:sdr,sdr_user_id:sdr,broker_user_id:String(form.get("broker_user_id")||"")||null,priority:String(form.get("priority")||"normal"),temperature:String(form.get("temperature")||"morno"),lead_score:score,budget_min:Number(form.get("budget_min")||0)||null,budget_max:budgetMax||null,preferred_area_min:Number(form.get("preferred_area_min")||0)||null,preferred_area_max:Number(form.get("preferred_area_max")||0)||null,preferred_city:String(form.get("preferred_city")||"")||null,financing_interest:form.get("financing_interest")==="on",payment_capacity:Number(form.get("payment_capacity")||0)||null,tags:String(form.get("tags")||"").split(",").map(v=>v.trim()).filter(Boolean),utm_source:String(form.get("utm_source")||"")||null,utm_medium:String(form.get("utm_medium")||"")||null,utm_campaign:String(form.get("utm_campaign")||"")||null,notes:String(form.get("notes")||"")||null,sla_due_at:new Date(now.getTime()+Number(stage?.sla_hours||24)*3600000).toISOString(),stagnation_at:now.toISOString(),...buyerPayload(form),created_by:lead?.created_by||data.session.user.id,updated_at:now.toISOString()}};}
